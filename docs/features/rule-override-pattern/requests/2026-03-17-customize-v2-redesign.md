# Request: --customize v2 Incremental Redesign

> **Created**: 2026-03-17
> **Status**: Completed
> **Priority**: P1
> **Tech Spec**: [3-customize-v2.md](../3-customize-v2.md)

## Background

v1 `--customize` wizard 只支援 4 個 preset 維度（code review / doc review / precommit / P2 sweep），為全量覆寫模式。使用者最常見需求——新增專案特有規則——無法透過 wizard 完成。`/best-practices` audit（Helm / Kustomize / ESLint / Tailwind 對比研究 + adversarial debate）確認應轉型為 incremental section operations。

## Requirements

- 重新設計 `--customize` 為三路操作：`--add-section` / `--update-section` / `--reset`
- 預設模式（無 sub-flag）為 read-only status table，列出所有可覆寫 sections
- 支援 freeform 自訂 section（不限於 base 內的 headings）
- Sentinel-based content detection（取代 heading 偵測，修復 false positive）
- 統一 hash 語義為 blob hash end-to-end
- 新增 `/claude-health` within-file duplicate heading P2 warning

## Scope

| Scope | Description |
|-------|-------------|
| In | `--customize` 三路操作重寫、sentinel detection、hash unification、duplicate heading health check |
| Out | 拆分 base `##` headings（defer v3）、擴展到其他 rules（仍只 `auto-loop`）、smart merge 演算法修改 |

## Related Files

| File | Action | Description |
|------|--------|-------------|
| `commands/install-rules.md:86-228` | Rewrite | 替換 4-preset wizard 為三路操作 |
| `rules/auto-loop-project.md` | Modify | 新增 `<!-- Generated by -->` sentinel |
| `skills/claude-health/SKILL.md:183-190` | Extend | 新增 duplicate heading warning (#5) |
| `commands/install-rules.md:3, 76-84, 495-518` | Modify | 更新 argument-hint + argument table + examples |
| `test/commands/install-rules-customize.test.js` | New | Section parsing + add/update/reset + mutual exclusion tests |
| `test/scripts/claude-health-override.test.js` | New | 新增 duplicate heading warning test cases |
| `README*.md` | Modify | 更新 4-preset wizard 相關描述（if any） |

## Acceptance Criteria

- [x] `--customize auto-loop`（無 sub-flag）輸出 status table 且不修改檔案
- [x] `--add-section` 正確 append section 且不破壞已有內容
- [x] `--add-section` 重複 heading 偵測 → error + redirect to `--update-section`
- [x] `--add-section` 自訂 heading input validation（level `##` only / length 3-80 / alphanumeric+spaces+hyphens+parens / reject newlines, `<!--`, empty/space-only）
- [x] `--update-section "X"` 只替換目標 section，保留其餘內容
- [x] `--reset` 需 AskUserQuestion 確認後才執行全量重新產生
- [x] Sub-flag 互斥驗證（`--add-section` + `--reset` → error；`--add-section` + `--update-section` → error；`--update-section` + `--reset` → error）
- [x] `--customize` + `--all`/`--list`/`--force`/`--dry-run`/`--legacy-strategy`/`rule-names` → Mode Gate error（繼承 v1）
- [x] Argument table 包含 `--add-section`, `--update-section`, `--reset` 三個 sub-flag 說明
- [x] Sentinel detection 正確處理 multiline HTML comments
- [x] 無 sentinel 的 v1 legacy files fallback to heading detection
- [x] Hash 統一為 blob hash（`Based on` emit + health check compare）
- [x] `/claude-health` duplicate heading P2 warning 正確觸發
- [x] Section parser 跳過 fenced code block 內的 `##`
- [x] Pass `/codex-review-doc`
- [x] Pass `/precommit-fast`

## Progress

| Phase | Status | Notes |
|-------|--------|-------|
| Feasibility | ✅ Done | `/best-practices` audit threadId: `019cfa99-3cba-7601-887e-c9b1f9e8adf8` |
| Tech Spec | ✅ Done | `3-customize-v2.md` — Codex review ✅ Mergeable |
| Implementation | ✅ Done | 7/7 tasks complete: customize rewrite, sentinel, health check #5, hash unification, args+examples, 21 tests, READMEs (no change needed) |
| Testing | ✅ Done | 120 tests pass (99 existing + 21 new) |
| Review | ✅ Done | `/codex-review-doc` ✅ Mergeable (threadId: `019cfb04-b6dd-7a10-8f3f-e06669fc7c75`) + `/precommit-fast` ✅ All Pass |

## References

- Tech Spec: [3-customize-v2.md](../3-customize-v2.md)
- v1 Tech Spec: [2-tech-spec.md](../2-tech-spec.md)
- v1 Request: [2026-03-16-rule-override-pattern.md](./2026-03-16-rule-override-pattern.md)
- Best Practices Audit: threadId `019cfa99-3cba-7601-887e-c9b1f9e8adf8`
